{
  "name": "vitest-hands-on",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "main": "templates/template/main.js",
  "type": "module",
  "workspaces": [
    "templates/*"
  ],
  "scripts": {
    "test": "vitest run src && npm test --workspaces",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "vitest": "^2.0.4",
    "prettier": "^3.3.3"
  }
}
